# `$channelID`

**Category:** Channels  
**Returns:** The ID of the channel where the command was used.

---

## 📝 Description

Returns the unique Discord channel ID of the channel the command was executed in. This is useful when passing channel references to other functions like `$sendMessage`, or logging interactions based on location.

---

## ⚙️ Usage

```txt
$channelID
```